services:
  vitarn-vidya:
    build: .
    container_name: vitarn-vidya
    restart: unless-stopped
    ports:
      - "3003:3003"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3003
    networks:
      - vitarn-network

networks:
  vitarn-network:
    driver: bridge
