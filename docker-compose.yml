services:
  vitarn-vidya:
    build: .
    container_name: vitarn-vidya
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3001
    networks:
      - vitarn-network

networks:
  vitarn-network:
    driver: bridge
