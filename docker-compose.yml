services:
  vitarn-vidya:
    build: .
    container_name: vitarn-vidya
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
    networks:
      - vitarn-network

networks:
  vitarn-network:
    driver: bridge
